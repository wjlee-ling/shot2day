export interface ImageItem {
  id: string;
  // title: string;
  // description: string;
  imageUrl: string;
  // createdAt: string;
}

export interface ImageMetadata {
  id: string;
  text: string;
  file_metadata: object;
}
